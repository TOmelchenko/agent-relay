apiVersion: v1
kind: Secret
metadata:
  name: agent-relay-db
type: Opaque
stringData:
  # "postgres" resolves via the cluster's Service DNS -- same reasoning as
  # the postgres hostname used in compose.yaml.
  RELAY_DATABASE_URL: postgresql+psycopg://agent_relay:agent_relay@postgres:5432/agent_relay
